{
    "allowed_policies": [
{% for policy in vault_policys %}
        "{{policy.name}}"{{ "," if not loop.last else "" }}
{% endfor %}
    ],
    "token_explicit_max_ttl": 0,
    "name": "nomad-cluster",
    "orphan": true,
    "period": 259200,
    "renewable": true
}
